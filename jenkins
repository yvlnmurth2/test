pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                echo 'building ${JOB_NAME}'
                echo 'Hello world!' 
            }
    dockerfile {
            filename 'docker'
            dir 'build'
            label 'my-docker'
            args '-v /tmp:/tmp'
        }
        }
    }
    post{
        success{
            echo 'sending email!' 
        }
    }
}
